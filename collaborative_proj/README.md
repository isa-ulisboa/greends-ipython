Create one file inside `submissions/`, named after your group, for example `team_03.py`. Inside that file, define a function called `run()`.

Example student file:

### submissions/team_03.py

    def run():
            return "Hello from team 03"
